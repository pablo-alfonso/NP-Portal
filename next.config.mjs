/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/NP-Portal',
  assetPrefix: '/NP-Portal/',
  images: { unoptimized: true },
};

export default nextConfig;
